import type { NextConfig } from 'next'

// Intentionally minimal — the only Vite setting that mattered was the dev port,
// preserved via `next dev -p 5174` in package.json. Add config here as needed.
const nextConfig: NextConfig = {}

export default nextConfig
