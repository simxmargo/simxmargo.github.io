node_modules
*.local

# Next.js build output + generated types
.next/
out/
next-env.d.ts
tsconfig.tsbuildinfo

# Secrets
.env
.env.local

# Supabase MCP config holds a Personal Access Token — never commit it.
# Commit .mcp.json.example (redacted) instead.
.mcp.json

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db

# Supabase local
supabase/.branches
supabase/.temp

# Pre-Next-migration restore point
.backup-premigration.tar.gz

# Offline Supabase backups (npm run backup) — hold PII (inquiries, contacts)
# and the repo is PUBLIC. Never commit; copy somewhere private instead.
backups/

# Playwright MCP snapshots
.playwright-mcp/
